# Copy this file to .env and fill in real values.
# Generate a secret with:
#   docker compose -f docker-compose-dev.yml exec main_app_rails ruby -rsecurerandom -e "puts SecureRandom.hex(64)"
JWT_SECRET_KEY=
